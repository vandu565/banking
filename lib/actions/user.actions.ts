'use server'

export const signIn= async()=> {

    try{
        //mutation/database/make api fetch
    }catch(error){
        console.error('Error',error);
    }

}

export const signUp = async (userData: SignUpParams) => {
  try {
    // create a user account 
  } catch (error) {
    console.error("Error", error);
  }
};